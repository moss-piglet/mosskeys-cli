[package]
name = "mosskeys-cli"
description = "mosskeys CLI and daemon: publish public keys, sign checkpoints locally (BYOK), and verify key history read-only and offline."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[[bin]]
name = "mosskeys"
path = "src/main.rs"

[dependencies]
mosskeys-core = { version = "0.2.1", path = "../mosskeys-core" }
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
base64.workspace = true

[dev-dependencies]
metamorphic-crypto.workspace = true
metamorphic-log.workspace = true
base64 = "0.22"
