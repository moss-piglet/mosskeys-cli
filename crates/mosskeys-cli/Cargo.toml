[package]
name = "mosskeys-cli"
description = "The MossKeys CLI + daemon: publish public key material and sign checkpoints locally (BYOK) against the owner-scoped write API."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[[bin]]
name = "mosskeys"
path = "src/main.rs"

[dependencies]
mosskeys-core = { version = "0.1.1", path = "../mosskeys-core" }
clap.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
metamorphic-crypto.workspace = true
metamorphic-log.workspace = true
base64 = "0.22"
